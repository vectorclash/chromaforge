import React from 'react';
import PageContainer from '../components/ui/PageContainer';

// Placeholder -- real gallery (public/mine, thumbnails, delete, load) lands in the
// flow-porting phase.
export default function GalleryPage() {
  return (
    <PageContainer title="Gallery" subtitle="Designs saved by the community and by you.">
      <p className="text-neutral-500">Saved designs coming soon.</p>
    </PageContainer>
  );
}
